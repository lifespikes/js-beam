import resolve from './utils/resolve';
import config from './utils/config';
import livrApp from './utils/inertia';
import http from './utils/http';

export default {
  resolve,
  config,
  livrApp,
  http,
};
